/**
 * 预留脚本。
 *
 * 当前站点使用 VitePress 内置的 `search.provider: 'local'`
 * （见 docs/.vitepress/config.ts），索引在构建时自动生成，
 * 无需手动运行任何脚本。
 *
 * 如果未来切换到 Algolia DocSearch 或自建全文搜索服务，
 * 可以在这里实现自定义索引生成逻辑。
 */
export {}
